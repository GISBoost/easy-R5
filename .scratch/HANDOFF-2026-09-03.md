# Handoff — Easy-R5, 2026-09-03 (session paused at 5h limit)

## State: everything committed on `main`, tree clean

M1–M5 implemented. v0.1.0, `experimental=True`. 8 algorithms, 120 pytest green,
flake8 clean. Last commits: `b4c5f16` (M3) … `0f40902` (OD-line CRS).

**Post-review round 2 (2026-09-03 pm):**
- **Isochrones rewritten** — Michał asked about a "native isochrone tool".
  Checked the 7.6 jar + bindings: **R5 has none**; r5r `isochrone()` = grid +
  `isoband` contour in R, r5py = grid + shapely, Conveyal = grid + browser
  contour. So grid-then-contour IS the standard. Replaced the crude
  buffer-union with the proper thing: full grid of times (sentinel for
  unreachable) → `qgis:tininterpolation` raster → `gdal:contour_polygon`
  per cutoff (`INTERVAL 0`, `EXTRA "-fl <c>"`), union bands ≤ c, drop rings
  < ~4 cells (noise) keep the rest (real pockets). One contour run per cutoff.
- **All vector outputs now follow the input CRS** (accessibility, isochrones,
  OD lines). Matrix CSV stays lon/lat by design.
- **PL translation 213/213**, all contexts, placeholders preserved.
- GitHub #1 (translation → human-pass) and #2 (isochrone quality → fixed)
  commented; KNOWN_ISSUES updated.

Plugin synced into the QGIS profile plugins dir and reloads clean; all 8
algorithms register; PL `.qm` loads and translates every context.

## Done this session (M3 → M5 loop + post-review)

- **M3** RunTravelTimeMatrix — full Gdańsk 1389×956 in 1:43, origin 0 matches Probe.
- **M4** RunAccessibility — reproduces r5r's Gdańsk output **exactly** (date 2026-08-24,
  27 780 rows, RMSE 0.00). `docs/notes/validation-gdansk.md`.
- **M5** GenerateIsochrones (blob-union, holes kept — merit-correct), PreparePopulationLayer
  + PopulationOverlay (ports, openpyxl bootstrap confirmed), styles auto-apply,
  full PL translation (212/212), README + `docs/img/isochrones-gdansk.png`,
  KNOWN_ISSUES.md + GitHub issues #1 #2.
- **CRS audit** — invalid-CRS guard in points.py; isochrones output in origin CRS;
  verified with EPSG:2180 inputs.

## Left to do (for the human / next session)

1. **Human verification on a clean QGIS profile** (agent cannot):
   - Install `easy_r5-0.1.0.zip` on a fresh profile → all 8 algorithms in the toolbox.
   - `DownloadR5` — real Adoptium + GitHub Releases download, no admin rights, SHA-256.
   - `BuildNetwork` on a large PBF — wall time, peak RAM, cache hit on re-run,
     no orphaned `java.exe` on cancel, OOM → readable message.
   - `TestR5Setup` output.
   - Run the matrix / accessibility / isochrones from the QGIS **dialog** (not just
     `processing.run`) on Michał's own layers; spot-check OD pairs vs the operator's
     journey planner.
   - Compare the Gdańsk accessibility map to `tools/accessibility_cities/out/` — same
     spatial pattern? (numbers already exact.)
2. **If all M1–M5 criteria pass** → flip `metadata.txt` `experimental=False`, keep
   `version=0.1.0` (or bump as you see fit), rebuild ZIP.
3. **Polish translation** is fully populated (213/213) but machine-translated
   (local LLM). A few awkward spots (`konturuje`, `cutoff_min` → "minimalny próg",
   "partia przetwarzania"). Human polish pass → issue #1. To rebuild after edits:
   `pylupdate5 -noobsolete easy_r5.pro` → `py tools/i18n/build_matrixbase_context.py`
   → `py <scratch>/m3/translate_full.py` (fills only unfinished) → `lrelease`.
   `lrelease`: no binary in this QGIS; used `pyside6-essentials` (`pip install --target`
   to scratchpad `pyside/`, then `pyside/PySide6/lrelease.exe`).
4. **Minor / optional**:
   - Isochrone `_run_matrix` still logs "N of M destinations not near any street" for
     the grid — expected (grid over sea / past the network), just noisy. Could thread a
     `quiet_unlinked` flag through `_run_matrix` → the Java `WARN UNLINKED_POINTS`.
   - `_MAX_GRID_POINTS = 400_000` in generate_isochrones.py — the guard uses the
     pre-clip bbox estimate (conservative).
   - Per-origin isochrone cost = 1 TIN + N contour runs. Fine for a few origins; for
     many-origin isochrone jobs consider a shared raster per batch.

## Environment notes

- Java/R5: `C:\Users\Michal\easy-r5\` (jdk-21.0.12.1+1, r5-v7.6-all.jar, runner_cache).
  QSettings `[easy_r5]` populated. Runner recompiled there after Java edits.
- Test network built by the agent: scratchpad
  `.../b8396830-.../scratchpad/m3/net/network.dat` (nv5, Gdańsk, service_days merged).
- Reference data: `tools/accessibility_cities/gdansk/`.
- Plugin install copy: `%APPDATA%\QGIS\QGIS3\profiles\default\python\plugins\easy_r5\`
  — sync files there + recompile runner into `~/easy-r5/runner_cache\` after edits.
