[flake8]
# Repo-root dev config (not packaged into the plugin ZIP).
max-line-length = 200
extend-ignore = E203, W503, W504
exclude = .git,__pycache__,docs,tools,reference

[tool:pytest]
testpaths = easy_r5/test
